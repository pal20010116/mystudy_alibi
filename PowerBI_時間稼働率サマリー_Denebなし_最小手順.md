# Power BI 時間稼働率サマリー作成手順書

## Denebなし・標準ビジュアル最小版

---

## 0. この手順書の位置づけ

この手順書は、すでに以下のタイムチャートを作成した後の続きとして作る前提です。

- 号機別 1日停止タイムライン
- 選択号機 × 複数日停止タイムチャート
- `Dim_号機`
- `Dim_故障名`
- `Dim_工場日付`
- `Dim_停止時間区分`
- `稼働明細` と各Dimのリレーション
- `稼働明細[停止時間_分]`
- `稼働明細[停止開始時刻_表示]`
- `稼働明細[停止終了時刻_表示]`

今回作るのは、**1ページ目の時間稼働率サマリー画面**です。

今回は Deneb を使いません。

Power BI標準ビジュアルだけで、次の3つを作ります。

```text
1. 号機別 時間稼働率サマリー
2. ワースト停止イベント TOP10
3. 選択号機 長時間停止 TOP3
```

---

## 1. 今回の最小構成

今回追加するものは以下です。

| 種類 | 作成するもの | 作成場所 |
|---|---|---|
| メジャー | 時間稼働率関係 | Measures テーブル |
| ビジュアル | 号機別サマリー | マトリックス |
| ビジュアル | ワースト停止TOP10 | テーブル |
| ビジュアル | 選択号機TOP3 | テーブル |

今回追加しないものは以下です。

| 追加しないもの | 理由 |
|---|---|
| 新しい計算列 | 既にタイムチャート作成時の列を使うため |
| Deneb JSON | 今回はPower BI標準ビジュアルで作るため |
| `Dim_時間稼働率ランク` | 最小版では条件付き書式のルールで色分けするため |
| 色メジャー | 最小版では作らない |
| 最長停止メジャー | TOP10 / TOP3 テーブルで確認できるため |

---

## 2. 今回は「列追加」ではなく「メジャー作成」

今回作る `時間稼働率` は、`稼働明細` に新しい列を追加して作るものではありません。

作るのは **メジャー** です。

```text
計算列：
  データ1行ごとに値を持つ
  例：稼働明細[停止時間_分]

メジャー：
  画面のフィルター条件に応じて、その場で集計結果を返す
  例：[時間稼働率]
```

今回の時間稼働率は、号機や工場日付の選択状態によって変わる集計値です。

そのため、`稼働明細` に列を追加するのではなく、`Measures` テーブルにメジャーとして作成します。

---

## 3. 完成画面

4台データの場合は、以下のような構成にします。

```text
┌────────────────────────────────────────────┐
│ 工場日付　停止時間区分　故障名              │
└────────────────────────────────────────────┘

┌────────────────────────────────────────────┐
│ 号機別 時間稼働率サマリー                   │
│ 1号機   94.2%   84分                        │
│ 2号機   87.5%   180分                       │
│ 3号機   76.3%   341分                       │
│ 4号機   68.9%   448分                       │
└────────────────────────────────────────────┘

┌─────────────────────┬──────────────────────┐
│ ワースト停止TOP10    │ 選択号機TOP3          │
└─────────────────────┴──────────────────────┘
```

---

## 4. 前提として使う既存列

今回新しく列は作りません。

以下は、タイムチャート作成時に既に作成済みの前提です。

| テーブル | 列 | 用途 |
|---|---|---|
| `稼働明細` | `停止時間` | 停止時間。単位は秒 |
| `稼働明細` | `停止時間_分` | TOP10 / TOP3 の表示と上位N判定 |
| `稼働明細` | `停止開始時刻_表示` | 停止開始時刻の表示 |
| `稼働明細` | `停止終了時刻_表示` | 停止終了時刻の表示 |
| `Dim_号機` | `号機名` | 号機表示 |
| `Dim_号機` | `号機表示順` | 号機の並び替え |
| `Dim_故障名` | `故障名` | 故障名表示 |
| `Dim_工場日付` | `工場日付` | 工場日付スライサー |
| `Dim_停止時間区分` | `停止時間区分` | 停止時間区分スライサー |

---

## 5. 既存リレーションの確認

以下のリレーションが既にある前提です。

```text
Dim_号機[号機Key]
  1 ── *
稼働明細[号機Key]

Dim_故障名[故障名Key]
  1 ── *
稼働明細[故障名Key]

Dim_工場日付[工場日付]
  1 ── *
稼働明細[工場日付]

Dim_停止時間区分[停止時間区分Key]
  1 ── *
稼働明細[停止時間区分Key]
```

すべて以下の設定にします。

```text
カーディナリティ：1対多
クロスフィルター方向：単一
アクティブ：はい
```

---

## 6. 作成するメジャー一覧

作成場所はすべて `Measures` テーブルです。

| メジャー名 | 用途 |
|---|---|
| `選択工場日付数` | 選択されている工場日付数を数える |
| `計画稼働秒` | 分母。24時間 × 選択工場日付数 |
| `停止時間合計_秒` | 時間稼働率用の停止時間合計 |
| `停止時間合計_分` | 表示用の停止時間合計 |
| `時間稼働率` | 号機別サマリーの主指標 |

---

## 7. メジャーを作成する

### 7.1 選択工場日付数

```DAX
選択工場日付数 =
DISTINCTCOUNT ( 'Dim_工場日付'[工場日付] )
```

ページ1は1日選択が基本ですが、複数日選択されても計算が破綻しないようにします。

---

### 7.2 計画稼働秒

```DAX
計画稼働秒 =
24 * 60 * 60 * [選択工場日付数]
```

1工場日付を、7:00〜翌7:00の24時間として扱います。

---

### 7.3 停止時間合計_秒

```DAX
停止時間合計_秒 =
COALESCE (
    CALCULATE (
        SUM ( '稼働明細'[停止時間] ),
        '稼働明細'[停止時間] > 0,
        REMOVEFILTERS ( 'Dim_停止時間区分' ),
        REMOVEFILTERS ( 'Dim_故障名' )
    ),
    0
)
```

このメジャーでは、以下のフィルターを残します。

```text
Dim_号機
Dim_工場日付
```

以下のフィルターは外します。

```text
Dim_停止時間区分
Dim_故障名
```

理由は、時間稼働率は「その号機・その工場日付の全停止時間」で計算するためです。

停止時間区分スライサーや故障名スライサーで時間稼働率まで変わると、稼働率の意味がズレます。

---

### 7.4 停止時間合計_分

```DAX
停止時間合計_分 =
DIVIDE ( [停止時間合計_秒], 60 )
```

号機別サマリーに表示するためのメジャーです。

---

### 7.5 時間稼働率

```DAX
時間稼働率 =
VAR PlannedSeconds = [計画稼働秒]
VAR StopSeconds = [停止時間合計_秒]
RETURN
MAX (
    0,
    DIVIDE ( PlannedSeconds - StopSeconds, PlannedSeconds )
)
```

表示形式は以下にします。

```text
パーセンテージ
小数点1桁
```

---

## 8. スライサーを配置する

ページ上部に以下の3つだけ置きます。

| スライサー | フィールド |
|---|---|
| 工場日付 | `Dim_工場日付[工場日付]` |
| 停止時間区分 | `Dim_停止時間区分[停止時間区分]` |
| 故障名 | `Dim_故障名[故障名]` |

号機スライサーは最小版では置きません。

号機の選択は、号機別サマリーの行クリックで行います。

---

## 9. 号機別 時間稼働率サマリーを作る

Power BI標準の **マトリックス** を追加します。

### 9.1 フィールド設定

行に入れるもの：

```text
Dim_号機[号機名]
```

値に入れるもの：

```text
[時間稼働率]
[停止時間合計_分]
```

### 9.2 表示名

表示上の名前は以下にします。

| 元の名前 | 表示名 |
|---|---|
| `[時間稼働率]` | 時間稼働率 |
| `[停止時間合計_分]` | 停止時間[分] |

### 9.3 並び順

`Dim_号機[号機名]` を `Dim_号機[号機表示順]` で並び替えます。

```text
Dim_号機[号機名]
→ 列で並べ替え
→ Dim_号機[号機表示順]
```

### 9.4 条件付き書式

`[時間稼働率]` に背景色を付けます。

```text
マトリックスを選択
→ 値 の [時間稼働率] の ▼
→ 条件付き書式
→ 背景色
```

設定は以下です。

```text
書式スタイル：ルール
基準にするフィールド：[時間稼働率]
```

ルールは以下です。

| 条件 | 色の目安 |
|---|---|
| 0.90以上 | 緑系 |
| 0.80以上 0.90未満 | 黄系 |
| 0.70以上 0.80未満 | 橙系 |
| 0.70未満 | 赤系 |

---

## 10. ワースト停止イベント TOP10 を作る

Power BI標準の **テーブル** を追加します。

### 10.1 フィールド設定

以下を入れます。

```text
Dim_号機[号機名]
Dim_故障名[故障名]
稼働明細[停止開始時刻_表示]
稼働明細[停止終了時刻_表示]
稼働明細[停止時間_分]
Dim_停止時間区分[停止時間区分]
```

### 10.2 上位Nフィルター

このテーブルでは、別途 `稼働明細[停止時間] > 0` のフィルターは必須にしません。

停止イベントの絞り込みは、`稼働明細[停止時間_分]` の上位Nだけで行います。

設定は以下です。

```text
フィルター
→ 稼働明細[停止時間_分]
→ フィルターの種類：上位N
→ 上位 10
→ 基準：稼働明細[停止時間_分]
→ フィルターを適用
```

### 10.3 並び替え

```text
稼働明細[停止時間_分]
→ 降順
```

### 10.4 タイトル

```text
ワースト停止イベント TOP10
```

この表は、以下のスライサーで変わります。

```text
工場日付
停止時間区分
故障名
```

---

## 11. 選択号機 長時間停止 TOP3 を作る

Power BI標準の **テーブル** を追加します。

### 11.1 フィールド設定

以下を入れます。

```text
Dim_故障名[故障名]
稼働明細[停止開始時刻_表示]
稼働明細[停止終了時刻_表示]
稼働明細[停止時間_分]
Dim_停止時間区分[停止時間区分]
```

号機名は入れなくてよいです。

この表は、号機別サマリーで選択した号機の中だけを見るためです。

### 11.2 上位Nフィルター

このテーブルでも、絞り込みは `稼働明細[停止時間_分]` の上位Nだけで行います。

```text
フィルター
→ 稼働明細[停止時間_分]
→ フィルターの種類：上位N
→ 上位 3
→ 基準：稼働明細[停止時間_分]
→ フィルターを適用
```

### 11.3 並び替え

```text
稼働明細[停止時間_分]
→ 降順
```

### 11.4 タイトル

```text
選択号機 長時間停止 TOP3
```

---

## 12. 相互作用を設定する

号機別サマリーで号機をクリックしたとき、TOP3だけがその号機に絞られるようにします。

```text
号機別サマリーのマトリックスを選択
→ 書式
→ 相互作用を編集
```

設定は以下です。

| 操作元 | 操作先 | 設定 |
|---|---|---|
| 号機別サマリー | 選択号機 長時間停止 TOP3 | フィルター |
| 号機別サマリー | ワースト停止イベント TOP10 | なし |

TOP10は全号機横断で見るため、号機別サマリーのクリックで絞らない設定にします。

---

## 13. レイアウト

4台データでは以下の配置にします。

```text
上部：
  工場日付スライサー
  停止時間区分スライサー
  故障名スライサー

中央：
  号機別 時間稼働率サマリー

下部左：
  ワースト停止イベント TOP10

下部右：
  選択号機 長時間停止 TOP3
```

画面イメージです。

```text
┌────────────────────────────────────────────┐
│ 工場日付　停止時間区分　故障名              │
└────────────────────────────────────────────┘

┌────────────────────────────────────────────┐
│ 号機別 時間稼働率サマリー                   │
└────────────────────────────────────────────┘

┌─────────────────────┬──────────────────────┐
│ ワースト停止TOP10    │ 選択号機TOP3          │
└─────────────────────┴──────────────────────┘
```

---

## 14. 最終確認

以下を確認します。

```text
1. 工場日付を選ぶと、号機別サマリーが変わる
2. 号機別サマリーに4台分の時間稼働率が出る
3. 時間稼働率が0%になっていない
4. 停止時間区分スライサーを変えても、時間稼働率は変わらない
5. 故障名スライサーを変えても、時間稼働率は変わらない
6. 停止時間区分スライサーを変えると、TOP10 / TOP3 は変わる
7. 故障名スライサーを変えると、TOP10 / TOP3 は変わる
8. TOP10は、停止時間_分 の上位10件だけで表示できる
9. TOP3は、停止時間_分 の上位3件だけで表示できる
10. 号機別サマリーで号機をクリックすると、TOP3だけがその号機に絞られる
11. TOP10は全号機横断のまま表示される
```

---

## 15. 作成順まとめ

実際の作業順は以下です。

```text
1. Measures に5つのメジャーを作る
2. 上部に3つのスライサーを置く
3. マトリックスで号機別サマリーを作る
4. 時間稼働率に条件付き書式を付ける
5. テーブルでワースト停止TOP10を作る
6. 停止時間_分 の上位Nで TOP10 にする
7. テーブルで選択号機TOP3を作る
8. 停止時間_分 の上位Nで TOP3 にする
9. 号機別サマリーからTOP3だけに相互作用を効かせる
10. 表示確認する
```

---

## 16. まとめ

今回のページでは、タイムチャート作成時に作ったデータモデルをそのまま使います。

新しいデータ列は追加しません。

時間稼働率は、`Measures` テーブルに作成するメジャーで計算します。

TOP10 / TOP3 は、追加メジャーや追加列を使わず、`稼働明細[停止時間_分]` の **上位Nフィルターだけ** で作ります。

```text
時間稼働率サマリー：
  メジャーで作る

TOP10 / TOP3：
  停止時間_分 の上位Nで作る

表示：
  Power BI標準ビジュアルのみ

Deneb：
  今回は使わない
```
