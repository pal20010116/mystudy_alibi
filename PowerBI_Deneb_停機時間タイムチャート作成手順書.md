# Power BI / Deneb 停機時間タイムチャート作成手順書

## 停止時間区分Dim + Power BIスライサー + Denebフィルター「停止時間 > 0」方式

---

## 0. この手順書の目的

この手順書は、Power BI の Deneb を使って、朝7時切替の工場日付ベースで停機時間タイムチャートを作成する手順を、省略せずに整理したものです。

今回の最終方針は以下です。

```text
停止時間区分の表示 / 非表示
→ Power BI側の Dim_停止時間区分 スライサーで制御する

Deneb内チェックボックス
→ 使わない

Deneb内凡例クリック制御
→ 使わない

Deneb側のビジュアルフィルター
→ 停止時間 > 0 を設定する
```

この方式にした理由は以下です。

- Deneb内チェックボックスは、日付スライサーを変更すると初期状態に戻る。
- 凡例クリック方式は、Deneb / Vega-Lite の layered chart で `Duplicate signal name` エラーが出る場合がある。
- Power BI側の非接続Dim + `ALLSELECTED` + `FILTER` メジャー方式は、DirectQueryでは重くなりやすく、`rsQueryMemoryLimitExceeded` が出る場合がある。
- 停止時間区分を通常のディメンションとして持たせると、Power BI標準スライサーで安定して制御できる。
- Denebフィルターで `停止時間 > 0` にすると、停止時間が存在するイベントだけを停機バーとして描画できる。

---

## 1. 作成したい可視化

作成する図は、以下のようなタイムチャートです。

- 横軸は `7:00` から翌日 `7:00`。
- 縦軸は `工場日付 + 曜日`。
- 稼働中は緑色の帯。
- 停機時間は、停止時間の長さに応じて色分けする。
- 停機時間バーは、実際の停止秒数に応じた横幅で表示する。
- 停止バーにカーソルを合わせると、以下を表示する。
  - 号機
  - 停止開始時刻
  - 停止終了時刻
  - 停止時間［分］
  - 故障名
  - 停止時間区分
- 停止時間区分は、Power BIのスライサーで表示 / 非表示を切り替える。
- 日付スライサーを変更しても、停止時間区分スライサーの選択状態は保持する。

---

## 2. モデル全体方針

今回のモデルは、スタースキーマを意識して次のようにします。

```text
Dim_号機[号機Key]
  1 ── *
稼働明細[号機Key]

Dim_故障名[故障名Key]
  1 ── *
稼働明細[故障名Key]

Dim_工場日付[工場日付]
  1 ── *
稼働明細[工場日付]

Dim_停止時間区分[停止時間区分Key]
  1 ── *
稼働明細[停止時間区分Key]
```

重要なのは、`稼働明細` に表示名を持たせないことです。

```text
稼働明細に持たせるもの
→ 停止時間区分Key

Dim_停止時間区分に持たせるもの
→ 停止時間区分名、表示順、色
```

これにより、停止時間区分を通常のディメンションとして扱えます。

---

## 3. 前提テーブル

### 3.1 ファクトテーブル：稼働明細

`稼働明細` は DirectQuery で読み込んでいるファクトテーブルです。

| 列名 | 内容 |
|---|---|
| 号機Key | 号機ディメンションと接続するキー |
| 故障名Key | 故障名ディメンションと接続するキー |
| 工場日付 | 朝7時に切り替わる日付 |
| 日付 | カレンダー通りの日付 |
| 時刻 | 停止イベントの開始時刻 |
| 停止時間 | 停止していた秒数 |
| 停止時間区分Key | 停止時間を区分化したキー |

今回の可視化では、通常の `日付` ではなく、必ず `工場日付` を主軸にします。

### 3.2 Dim_号機

| 列名 | 内容 |
|---|---|
| 号機Key | 稼働明細と接続するキー |
| 号機名 | 表示用の号機名 |

列名が `号機` の場合は、JSON内の `号機名` を実際の列名に合わせて変更します。

### 3.3 Dim_故障名

| 列名 | 内容 |
|---|---|
| 故障名Key | 稼働明細と接続するキー |
| 故障名 | 表示用の故障名 |

### 3.4 Dim_工場日付

`Dim_工場日付` は、朝7時切替の工場日付を管理するディメンションです。

作成DAX例は以下です。

```DAX
Dim_工場日付 =
VAR MinWorkDate =
    MINX ( ALL ( '稼働明細' ), '稼働明細'[工場日付] )
VAR MaxWorkDate =
    MAXX ( ALL ( '稼働明細' ), '稼働明細'[工場日付] )
VAR StartDate =
    DATE ( YEAR ( MinWorkDate ) - 1, 1, 1 )
VAR EndDate =
    DATE ( YEAR ( MaxWorkDate ), 12, 31 )
RETURN
ADDCOLUMNS (
    CALENDAR ( StartDate, EndDate ),
    "工場日付", [Date],
    "年", YEAR ( [Date] ),
    "月", MONTH ( [Date] ),
    "日", DAY ( [Date] ),
    "曜日番号", WEEKDAY ( [Date], 2 ),
    "曜日", FORMAT ( [Date], "aaa", "ja-JP" ),
    "工場日付表示",
        FORMAT ( [Date], "yyyy/MM/dd", "ja-JP" )
            & "（"
            & FORMAT ( [Date], "aaa", "ja-JP" )
            & "）",
    "工場日付index",
        VALUE ( FORMAT ( [Date], "yyyymmdd", "ja-JP" ) )
)
```

各列の用途は以下です。

| 列名 | 用途 |
|---|---|
| 工場日付 | 稼働明細とリレーションするキー |
| 年 | 年スライサーや年集計に使用 |
| 月 | 月スライサーや月集計に使用 |
| 日 | 日単位の確認に使用 |
| 曜日番号 | 月曜=1、日曜=7。曜日順の並び替えに使用 |
| 曜日 | 月、火などの表示用 |
| 工場日付表示 | Denebの縦軸表示に使用 |
| 工場日付index | Denebの縦軸ソートに使用 |

---

## 4. 停止時間区分Keyを稼働明細に追加する

### 4.1 稼働明細に追加するのはKeyのみ

スタースキーマを守るため、`稼働明細` に入れるのは表示名ではなく、`停止時間区分Key` だけです。

```text
稼働明細[停止時間区分Key]
```

表示名である `5分未満`、`5〜15分` などは、`Dim_停止時間区分` 側に持たせます。

### 4.2 DAX計算列で作る例

DirectQueryで計算列が許可される環境であれば、以下の計算列を作成します。

```DAX
停止時間区分Key =
SWITCH (
    TRUE (),
    '稼働明細'[停止時間] < 300, 1,
    '稼働明細'[停止時間] < 900, 2,
    '稼働明細'[停止時間] < 1800, 3,
    '稼働明細'[停止時間] < 3600, 4,
    5
)
```

区分定義は以下です。

| 停止時間区分 | 停止時間条件 | Key |
|---|---:|---:|
| 5分未満 | 300秒未満 | 1 |
| 5〜15分 | 300秒以上、900秒未満 | 2 |
| 15〜30分 | 900秒以上、1800秒未満 | 3 |
| 30〜60分 | 1800秒以上、3600秒未満 | 4 |
| 60分以上 | 3600秒以上 | 5 |

### 4.3 DirectQueryで計算列が難しい場合

`稼働明細` が DirectQuery のため、DAX計算列が作れない、または重い場合があります。

その場合は、以下の順で検討します。

1. Power Query で条件列を追加できるか確認する。
2. クエリフォールディングされる形で停止時間区分Keyを追加する。
3. 既存データソース側に同等列があるならそれを使用する。
4. どうしても無理な場合は、Deneb内で停止時間区分を計算する。

ただし、今回の推奨は、Power BI標準スライサーを使うために、できるだけ `稼働明細[停止時間区分Key]` を用意することです。

---

## 5. Dim_停止時間区分を作成する

### 5.1 DAXテーブル

次の計算テーブルを作成します。

```DAX
Dim_停止時間区分 =
DATATABLE (
    "停止時間区分Key", INTEGER,
    "停止時間区分", STRING,
    "表示順", INTEGER,
    "色", STRING,
    {
        { 1, "5分未満", 1, "#1E88E5" },
        { 2, "5〜15分", 2, "#CDDC39" },
        { 3, "15〜30分", 3, "#FFC107" },
        { 4, "30〜60分", 4, "#FB8C00" },
        { 5, "60分以上", 5, "#D32F2F" }
    }
)
```

### 5.2 Dim_停止時間区分の列の意味

| 列名 | 用途 |
|---|---|
| 停止時間区分Key | 稼働明細とリレーションするキー |
| 停止時間区分 | スライサー、凡例、ツールチップに使う表示名 |
| 表示順 | 区分を意図した順に並べるための列 |
| 色 | 色管理用。必要に応じて使用 |

### 5.3 表示順で並び替え

Power BI のモデルビューまたはデータビューで、以下を設定します。

```text
Dim_停止時間区分[停止時間区分]
```

列で並べ替え：

```text
Dim_停止時間区分[表示順]
```

これにより、スライサーや凡例で以下の順になります。

```text
5分未満
5〜15分
15〜30分
30〜60分
60分以上
```

---

## 6. リレーションを作成する

モデルビューで以下のリレーションを作成します。

### 6.1 号機

```text
Dim_号機[号機Key]
  1 ── *
稼働明細[号機Key]
```

設定：

```text
カーディナリティ：1対多
クロスフィルター方向：単一
```

### 6.2 故障名

```text
Dim_故障名[故障名Key]
  1 ── *
稼働明細[故障名Key]
```

設定：

```text
カーディナリティ：1対多
クロスフィルター方向：単一
```

### 6.3 工場日付

```text
Dim_工場日付[工場日付]
  1 ── *
稼働明細[工場日付]
```

設定：

```text
カーディナリティ：1対多
クロスフィルター方向：単一
```

### 6.4 停止時間区分

```text
Dim_停止時間区分[停止時間区分Key]
  1 ── *
稼働明細[停止時間区分Key]
```

設定：

```text
カーディナリティ：1対多
クロスフィルター方向：単一
```

このリレーションにより、`Dim_停止時間区分` のスライサーで `稼働明細` を絞り込めます。

---

## 7. スライサーを作成する

### 7.1 工場日付スライサー

以下を使います。

```text
Dim_工場日付[工場日付]
```

通常の `稼働明細[日付]` は使いません。

理由は、今回の可視化が朝7時切替の工場日付基準だからです。

### 7.2 号機スライサー

以下を使います。

```text
Dim_号機[号機名]
```

列名が `号機` の場合は、実際の列名を使用します。

### 7.3 故障名スライサー

以下を使います。

```text
Dim_故障名[故障名]
```

### 7.4 停止時間区分スライサー

以下を使います。

```text
Dim_停止時間区分[停止時間区分]
```

このスライサーで、以下の区分を表示 / 非表示できます。

```text
5分未満
5〜15分
15〜30分
30〜60分
60分以上
```

Power BI標準スライサーなので、日付スライサーを変更しても選択状態は保持されます。

---

## 8. Denebビジュアルを配置する

1. レポート画面に Deneb ビジュアルを配置する。
2. Deneb の Values に必要なフィールドを入れる。
3. Vega-Lite の空テンプレートを選択する。
4. この手順書の JSON を貼り付ける。
5. `Apply changes` を押す。

---

## 9. Deneb Values に入れるフィールド

Deneb の Values には以下を入れます。

```text
Dim_工場日付[工場日付表示]
Dim_工場日付[工場日付index]
稼働明細[時刻]
稼働明細[停止時間]
Dim_号機[号機名]
Dim_故障名[故障名]
```

### 停止時間区分について

今回の JSON では、色分け用の区分表示を Deneb 内で `停止時間` から再計算します。

そのため、Deneb Values に `Dim_停止時間区分[停止時間区分]` を入れなくても動きます。

ただし、ツールチップに Power BI側の区分名をそのまま出したい場合は、以下を追加してもよいです。

```text
Dim_停止時間区分[停止時間区分]
```

この場合、JSON のツールチップに `停止時間区分` を追加します。

---

## 10. Denebフィルターを設定する

今回の重要ポイントです。

Deneb ビジュアルのフィルターペインで、以下を設定します。

```text
稼働明細[停止時間] > 0
```

または、Power BI の表示に合わせて、以下にします。

```text
停止時間 は 0 より大きい
```

### このフィルターを設定する理由

`停止時間` が `0` または空の行が Deneb に渡ると、停機バーや縦軸の表示が意図しない動きになる場合があります。

`停止時間 > 0` を設定することで、停機イベントとして意味のある行だけを Deneb に渡せます。

今回、号機選択時に日付範囲が意図せず短くなる問題も、このフィルター設定によって改善できました。

---

## 11. Deneb JSON 全体

以下を Deneb に貼り付けます。

> 画像原稿では JSON の冒頭部分のみが画面内に写っています。ここでは、画像から読み取れる冒頭部分、12章の計算式、9章の Values、10章のフィルター方針と整合する、Deneb に貼り付け可能な完全版として整理しています。

```json
{
  "$schema": "https://vega.github.io/schema/vega-lite/v5.json",
  "data": {
    "name": "dataset"
  },
  "autosize": {
    "type": "fit-x",
    "contains": "padding"
  },
  "transform": [
    {
      "calculate": "toDate(datum['時刻'])",
      "as": "開始時刻_Date"
    },
    {
      "calculate": "hours(datum['開始時刻_Date'])",
      "as": "開始時"
    },
    {
      "calculate": "minutes(datum['開始時刻_Date'])",
      "as": "開始分"
    },
    {
      "calculate": "seconds(datum['開始時刻_Date'])",
      "as": "開始秒"
    },
    {
      "calculate": "(datum['開始時'] < 10 ? '0' : '') + datum['開始時'] + ':' + (datum['開始分'] < 10 ? '0' : '') + datum['開始分'] + ':' + (datum['開始秒'] < 10 ? '0' : '') + datum['開始秒']",
      "as": "停止開始時刻_表示"
    },
    {
      "calculate": "datum['開始時'] < 7 ? datum['開始時'] + 24 : datum['開始時']",
      "as": "開始時_24"
    },
    {
      "calculate": "datum['開始時_24'] * 3600 + datum['開始分'] * 60 + datum['開始秒']",
      "as": "停止開始_総秒_24"
    },
    {
      "calculate": "datum['停止開始_総秒_24'] / 3600",
      "as": "停止開始_時刻値"
    },
    {
      "calculate": "datum['停止時間'] / 3600",
      "as": "停止時間_時間"
    },
    {
      "calculate": "datum['停止開始_総秒_24'] + datum['停止時間']",
      "as": "停止終了_総秒_24"
    },
    {
      "calculate": "datum['停止終了_総秒_24'] / 3600",
      "as": "停止終了_時刻値"
    },
    {
      "calculate": "floor(datum['停止終了_総秒_24'] / 3600)",
      "as": "終了時_24"
    },
    {
      "calculate": "floor((datum['停止終了_総秒_24'] % 3600) / 60)",
      "as": "終了分"
    },
    {
      "calculate": "floor(datum['停止終了_総秒_24'] % 60)",
      "as": "終了秒"
    },
    {
      "calculate": "datum['終了時_24'] >= 24 ? datum['終了時_24'] - 24 : datum['終了時_24']",
      "as": "終了時"
    },
    {
      "calculate": "(datum['終了時'] < 10 ? '0' : '') + datum['終了時'] + ':' + (datum['終了分'] < 10 ? '0' : '') + datum['終了分'] + ':' + (datum['終了秒'] < 10 ? '0' : '') + datum['終了秒']",
      "as": "停止終了時刻"
    },
    {
      "calculate": "datum['停止時間'] / 60",
      "as": "停止時間_分"
    },
    {
      "calculate": "datum['停止時間'] < 300 ? '5分未満' : datum['停止時間'] < 900 ? '5〜15分' : datum['停止時間'] < 1800 ? '15〜30分' : datum['停止時間'] < 3600 ? '30〜60分' : '60分以上'",
      "as": "停止時間区分_表示"
    }
  ],
  "width": "container",
  "height": 420,
  "layer": [
    {
      "transform": [
        {
          "aggregate": [
            {
              "op": "min",
              "field": "工場日付index",
              "as": "工場日付index"
            }
          ],
          "groupby": [
            "工場日付表示"
          ]
        }
      ],
      "mark": {
        "type": "bar",
        "color": "#4ec968",
        "opacity": 0.6,
        "cornerRadius": 0
      },
      "encoding": {
        "x": {
          "datum": 7,
          "type": "quantitative",
          "scale": {
            "domain": [
              7,
              31
            ]
          },
          "axis": {
            "title": "時刻",
            "values": [
              7,
              9,
              12,
              15,
              18,
              21,
              24,
              27,
              31
            ],
            "labelExpr": "datum.value >= 24 ? datum.value - 24 + ':00' : datum.value + ':00'"
          }
        },
        "x2": {
          "datum": 31
        },
        "y": {
          "field": "工場日付表示",
          "type": "nominal",
          "sort": {
            "field": "工場日付index",
            "order": "ascending"
          },
          "scale": {
            "paddingInner": 0.28,
            "paddingOuter": 0.18
          },
          "axis": {
            "title": null,
            "labelFontSize": 10,
            "labelLimit": 90,
            "labelPadding": 6
          }
        }
      }
    },
    {
      "mark": {
        "type": "bar",
        "cornerRadius": 0
      },
      "encoding": {
        "x": {
          "field": "停止開始_時刻値",
          "type": "quantitative",
          "scale": {
            "domain": [
              7,
              31
            ]
          },
          "axis": {
            "title": "時刻",
            "values": [
              7,
              9,
              12,
              15,
              18,
              21,
              24,
              27,
              31
            ],
            "labelExpr": "datum.value >= 24 ? datum.value - 24 + ':00' : datum.value + ':00'"
          }
        },
        "x2": {
          "field": "停止終了_時刻値"
        },
        "y": {
          "field": "工場日付表示",
          "type": "nominal",
          "sort": {
            "field": "工場日付index",
            "order": "ascending"
          },
          "scale": {
            "paddingInner": 0.28,
            "paddingOuter": 0.18
          },
          "axis": {
            "title": null,
            "labelFontSize": 10,
            "labelLimit": 90,
            "labelPadding": 6
          }
        },
        "color": {
          "field": "停止時間区分_表示",
          "type": "nominal",
          "scale": {
            "domain": [
              "5分未満",
              "5〜15分",
              "15〜30分",
              "30〜60分",
              "60分以上"
            ],
            "range": [
              "#1E88E5",
              "#CDDC39",
              "#FFC107",
              "#FB8C00",
              "#D32F2F"
            ]
          },
          "legend": {
            "title": "停止時間区分"
          }
        },
        "tooltip": [
          {
            "field": "号機名",
            "type": "nominal",
            "title": "号機"
          },
          {
            "field": "工場日付表示",
            "type": "nominal",
            "title": "工場日付"
          },
          {
            "field": "停止開始時刻_表示",
            "type": "nominal",
            "title": "停止開始時刻"
          },
          {
            "field": "停止終了時刻",
            "type": "nominal",
            "title": "停止終了時刻"
          },
          {
            "field": "停止時間_分",
            "type": "quantitative",
            "title": "停止時間[分]",
            "format": ".1f"
          },
          {
            "field": "故障名",
            "type": "nominal",
            "title": "故障名"
          },
          {
            "field": "停止時間区分_表示",
            "type": "nominal",
            "title": "停止時間区分"
          }
        ]
      }
    }
  ],
  "config": {
    "view": {
      "stroke": null
    },
    "axis": {
      "grid": true,
      "labelFontSize": 11,
      "titleFontSize": 12
    },
    "legend": {
      "labelFontSize": 11,
      "titleFontSize": 12
    }
  }
}
```

列名が異なる場合は、JSON内の以下のフィールド名を実データに合わせて変更します。

```text
工場日付表示
工場日付index
時刻
停止時間
号機名
故障名
```

---

## 12. Deneb側でのポイント

### 12.1 停止時間区分はDeneb内で再計算する

JSON内では以下で停止時間区分を作っています。

```json
{
  "calculate": "datum['停止時間'] < 300 ? '5分未満' : datum['停止時間'] < 900 ? '5〜15分' : datum['停止時間'] < 1800 ? '15〜30分' : datum['停止時間'] < 3600 ? '30〜60分' : '60分以上'",
  "as": "停止時間区分_表示"
}
```

これにより、Deneb側の色分けは、`停止時間` 秒数を基準に安定して行えます。

### 12.2 表示 / 非表示はPower BI側で行う

停止時間区分の表示 / 非表示は、以下のスライサーで行います。

```text
Dim_停止時間区分[停止時間区分]
```

このスライサーは、リレーションを通じて `稼働明細` を絞り込みます。

Deneb は、絞り込まれた `稼働明細` のデータを描画するだけです。

### 12.3 Denebフィルターで停止時間 > 0を設定する

Deneb ビジュアルに対して、Power BI のフィルターペインで以下を設定します。

```text
停止時間 > 0
```

この設定により、停止時間が0以下または空の行を除外できます。

---

## 13. 表示確認チェックリスト

### 13.1 モデル確認

- `稼働明細[停止時間区分Key]` が存在する。
- `Dim_停止時間区分[停止時間区分Key]` が存在する。
- `Dim_停止時間区分[停止時間区分]` が存在する。
- `Dim_停止時間区分[停止時間区分]` が `表示順` で並び替えされている。
- `Dim_停止時間区分` と `稼働明細` が Key でリレーションされている。

### 13.2 スライサー確認

- 工場日付スライサーは `Dim_工場日付[工場日付]` を使っている。
- 号機スライサーは `Dim_号機[号機名]` を使っている。
- 故障名スライサーは `Dim_故障名[故障名]` を使っている。
- 停止時間区分スライサーは `Dim_停止時間区分[停止時間区分]` を使っている。

### 13.3 Deneb確認

- Deneb Values に必要フィールドが入っている。
- Deneb のビジュアルフィルターで `停止時間 > 0` が設定されている。
- 横軸が `7:00〜27:00` になっている。
- 縦軸が `工場日付 + 曜日` になっている。
- 停止時間が実際の停止秒数に応じた横幅で表示される。
- 停止時間の色分けが正しい。
- 停止時間区分スライサーで表示 / 非表示が切り替わる。
- ツールチップに必要項目が表示される。

---

## 14. よくある問題と対応

### 14.1 停止時間区分スライサーを変えてもDenebが変わらない

確認すること：

1. `Dim_停止時間区分[停止時間区分Key]` と `稼働明細[停止時間区分Key]` が接続されているか。
2. リレーションがアクティブか。
3. クロスフィルター方向が `Dim_停止時間区分 → 稼働明細` に効く単一方向か。
4. スライサーに `Dim_停止時間区分[停止時間区分]` を使っているか。
5. Power BI の「相互作用を編集」で、スライサーが Deneb をフィルターしているか。

### 14.2 号機を変えると表示日付範囲が変わる

今回の方式では、Deneb のビジュアルフィルターで以下を設定します。

```text
停止時間 > 0
```

これにより、停止イベントとして意味のある行だけが表示され、号機選択時の不自然な表示変化を抑えられます。

ただし、`稼働明細` が停止イベントだけのテーブルである場合、停止がない日はそもそも行が存在しないため、停止がない日を完全に表示したい場合は、別途 `号機 × 工場日付` の表示用テーブルが必要になる場合があります。

### 14.3 緑背景しか表示されない

確認すること：

1. `稼働明細[停止時間]` が Deneb Values に入っているか。
2. Denebフィルターが `停止時間 > 0` になっているか。
3. `停止時間区分Key` のリレーションが効いているか。
4. 停止時間が集計されていないか。
5. JSON内の列名が、実際の Deneb データ列名と一致しているか。

### 14.4 ツールチップの号機が undefined になる

原因は、JSONの列名と Deneb に渡っている列名が一致していないことです。

JSONでは以下を参照しています。

```json
{
  "field": "号機名",
  "title": "号機"
}
```

もし実際の列名が `号機` の場合は、以下に変更します。

```json
{
  "field": "号機",
  "title": "号機"
}
```

### 14.5 日付を変えると停止時間区分の選択が戻る

Deneb内チェックボックス方式では、日付変更時にチェック状態が戻ります。

今回の方式では、Power BI標準スライサーで停止時間区分を選択するため、日付を変えても停止時間区分スライサーの選択状態は保持されます。

---

## 15. 最終作成手順まとめ

### 手順1：稼働明細に停止時間区分Keyを追加する

```DAX
停止時間区分Key =
SWITCH (
    TRUE (),
    '稼働明細'[停止時間] < 300, 1,
    '稼働明細'[停止時間] < 900, 2,
    '稼働明細'[停止時間] < 1800, 3,
    '稼働明細'[停止時間] < 3600, 4,
    5
)
```

### 手順2：Dim_停止時間区分を作る

```DAX
Dim_停止時間区分 =
DATATABLE (
    "停止時間区分Key", INTEGER,
    "停止時間区分", STRING,
    "表示順", INTEGER,
    "色", STRING,
    {
        { 1, "5分未満", 1, "#1E88E5" },
        { 2, "5〜15分", 2, "#CDDC39" },
        { 3, "15〜30分", 3, "#FFC107" },
        { 4, "30〜60分", 4, "#FB8C00" },
        { 5, "60分以上", 5, "#D32F2F" }
    }
)
```

### 手順3：リレーションを作る

```text
Dim_停止時間区分[停止時間区分Key]
  1 ── *
稼働明細[停止時間区分Key]
```

号機、故障名、工場日付のリレーションも確認します。

### 手順4：スライサーを作る

```text
Dim_工場日付[工場日付]
Dim_号機[号機名]
Dim_故障名[故障名]
Dim_停止時間区分[停止時間区分]
```

### 手順5：Denebを配置する

Power BI レポートに Deneb ビジュアルを配置します。

### 手順6：Deneb Values を設定する

```text
Dim_工場日付[工場日付表示]
Dim_工場日付[工場日付index]
稼働明細[時刻]
稼働明細[停止時間]
Dim_号機[号機名]
Dim_故障名[故障名]
```

### 手順7：Denebビジュアルフィルターを設定する

Deneb ビジュアルのフィルターペインで以下を設定します。

```text
稼働明細[停止時間] > 0
```

### 手順8：JSONを貼り付ける

この手順書の「11. Deneb JSON 全体」を Deneb に貼り付けます。

### 手順9：Apply changes を押す

Deneb では、JSON を貼った後に必ず `Apply changes` を押します。

### 手順10：表示確認する

以下を確認します。

- 横軸が `7:00〜翌7:00` になっている。
- 縦軸が `工場日付 + 曜日` になっている。
- 停止時間が実幅で表示されている。
- 停止時間の色分けが正しい。
- 停止時間区分スライサーで表示 / 非表示が切り替わる。
- 日付を変えても停止時間区分スライサーの選択が戻らない。
- ツールチップに必要項目が出る。

---

## 16. まとめ

今回の方式では、停止時間区分の制御を次のように整理します。

```text
停止時間区分の表示 / 非表示
→ Power BI標準スライサーで制御

停止時間区分の分類
→ 稼働明細[停止時間区分Key]

停止時間区分の表示名
→ Dim_停止時間区分[停止時間区分]

Denebの色分け
→ 停止時間から Deneb 内で停止時間区分表示を再計算

Denebの不要行除外
→ ビジュアルフィルターで 停止時間 > 0
```

この構成にすることで、以下を満たせます。

- スタースキーマを維持できる。
- 停止時間区分スライサーの選択状態が日付変更で戻らない。
- Deneb内チェックボックスを使わずに済む。
- DirectQueryで重くなりやすい非接続Dim + `ALLSELECTED` メジャーを使わない。
- Denebは描画に集中できる。
- 停止時間区分ごとの表示 / 非表示が Power BI標準機能で制御できる。

---

## 付録：原資料画像の対応

本手順書は、zipフォルダ内の以下の画像内容を基に作成しています。

| 画像 | 主な内容 |
|---|---|
| IMG_0808.JPG | 表紙、目的、最終方針、方式を選んだ理由 |
| IMG_0809.JPG | 方式を選んだ理由、作成したい可視化 |
| IMG_0810.JPG | モデル全体方針、スタースキーマ |
| IMG_0811.JPG | 前提テーブル、稼働明細、Dim_号機 |
| IMG_0812.JPG | Dim_号機、Dim_故障名、Dim_工場日付 |
| IMG_0813.JPG | Dim_工場日付のDAX例 |
| IMG_0814.JPG | Dim_工場日付の列用途、停止時間区分Key追加方針 |
| IMG_0815.JPG | 停止時間区分Key計算列、区分定義 |
| IMG_0816.JPG | DirectQueryで計算列が難しい場合、Dim_停止時間区分作成 |
| IMG_0817.JPG | Dim_停止時間区分DAXテーブル、列の意味 |
| IMG_0818.JPG | 表示順で並び替え、リレーション作成 |
| IMG_0819.JPG | 号機、故障名リレーション |
| IMG_0820.JPG | 工場日付、停止時間区分リレーション |
| IMG_0821.JPG | 工場日付スライサー、号機スライサー、故障名スライサー |
| IMG_0822.JPG | 故障名スライサー、停止時間区分スライサー、Deneb配置 |
| IMG_0823.JPG | Deneb配置、Deneb Values |
| IMG_0824.JPG | 停止時間区分、Denebフィルター設定 |
| IMG_0825.JPG | Denebフィルター理由、Deneb JSON冒頭 |
| IMG_0826.JPG | Deneb側のポイント、停止時間区分の再計算 |
| IMG_0827.JPG | Denebフィルター、表示確認チェックリスト |
| IMG_0828.JPG | スライサー確認、Deneb確認、よくある問題 |
| IMG_0829.JPG | よくある問題：スライサー反映、号機変更時の日付範囲 |
| IMG_0830.JPG | よくある問題：緑背景のみ、ツールチップundefined |
| IMG_0831.JPG | よくある問題：日付変更時の選択戻り、最終手順 |
| IMG_0832.JPG | Dim_停止時間区分、リレーション最終手順 |
| IMG_0833.JPG | スライサー、Deneb配置、Values、フィルター最終手順 |
| IMG_0834.JPG | JSON貼り付け、Apply changes、表示確認 |
| IMG_0835.JPG | 表示確認、まとめ |
| IMG_0836.JPG | まとめ |
